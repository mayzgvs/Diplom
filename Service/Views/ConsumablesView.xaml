<UserControl x:Class="Service.Views.ConsumablesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Service.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Utility/Style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="Управление расходными материалами" 
                       FontSize="28" 
                       FontWeight="Light" 
                       Foreground="{StaticResource DarkTextBrush}"
                       Margin="0,0,0,15"/>

            <Border CornerRadius="8" 
                    Padding="15"
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto" MinWidth="199"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Border Background="White" 
                                CornerRadius="5" 
                                BorderBrush="{StaticResource BorderBrush}" 
                                BorderThickness="1"
                                Margin="0,0,10,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" 
                                           Text="🔍" 
                                           Margin="10,8" 
                                           Foreground="{StaticResource LightTextBrush}"/>
                                <TextBox 
                                         Width="250" 
                                         BorderThickness="0"
                                         Background="Transparent"
                                         Padding="0,8,10,8"
                                         ToolTip="Введите наименование расходного материала"
                                         Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.ColumnSpan="2" Margin="36,0,0,0"/>
                            </Grid>
                        </Border>

                        <Border Background="White" 
                                CornerRadius="5" 
                                BorderBrush="{StaticResource BorderBrush}" 
                                BorderThickness="1"
                                Margin="0,0,10,0" Width="229">
                            <Grid Width="241">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" 
                                           Text="📋" 
                                           Margin="10,8" 
                                           Foreground="{StaticResource LightTextBrush}"/>
                                <ComboBox 
                                          BorderThickness="0"
                                          Background="Transparent"
                                          ItemsSource="{Binding Categories}"
                                          SelectedItem="{Binding SelectedCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                          ToolTip="Фильтр по категории" Margin="2,0,18,0" Grid.Column="1">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}" 
                                                       Margin="5,3"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <ListView Grid.Row="1" 
                  Margin="20,0,20,20"
                  ItemsSource="{Binding FilteredConsumables}"
                  SelectedItem="{Binding CurrentConsumable, Mode=TwoWay}"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  Background="White"
                  BorderBrush="{StaticResource BorderBrush}"
                  BorderThickness="1">

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" 
                                    DisplayMemberBinding="{Binding Id}"/>

                    <GridViewColumn Header="Наименование" Width="300" 
                                    DisplayMemberBinding="{Binding Name}"/>

                    <GridViewColumn Header="Категория" Width="250">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="15" Padding="8,3">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                            <Setter Property="Opacity" Value="0.1"/>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="● " 
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   FontSize="14"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding ConsumableCategory.Name}" 
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="8"/>
                    <Setter Property="MinHeight" Value="40"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="8" 
                Padding="15"
                Margin="20,0,20,20"
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="➕ Добавить"
                        Style="{StaticResource AddButtonWithIconStyle}" 
                        Width="120" 
                        Margin="0,0,10,0" 
                        Command="{Binding AddCommand}"/>

                <Button Content="✏️ Изменить"
                        Style="{DynamicResource EditButtonWithIconStyle}" 
                        Width="120" 
                        Margin="0,0,10,0" 
                        Command="{Binding EditCommand}"/>

                <Button Content="🗑️ Удалить"
                        Style="{DynamicResource DeleteButtonWithIconStyle}" 
                        Width="120" 
                        Command="{Binding DeleteCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>